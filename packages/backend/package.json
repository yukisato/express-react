{
  "name": "backend",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "start": "ts-node-dev --respawn src/index.ts",
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.9",
    "ts-node": "^9.1.1",
    "ts-node-dev": "^1.1.1"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
